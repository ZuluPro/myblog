---
machine:
  environment:
    BLOG_CONFIG_FILE: extras/myblog.cfg.ci
  services:
    - mysql
    - redis

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - pwd && ls -lah extras/
    - BLOG_CONFIG_FILE=extras/myblog.cfg.ci myblog/manage.py test
